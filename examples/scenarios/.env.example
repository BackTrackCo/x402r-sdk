# Private key for test wallet (NEVER use mainnet-funded keys)
# Generate a test key: cast wallet new
PRIVATE_KEY=0x...

# Base Sepolia RPC URL (optional — defaults to public endpoint)
RPC_URL=https://sepolia.base.org

# Chain ID (optional — defaults to Base Sepolia)
NETWORK_ID=84532
