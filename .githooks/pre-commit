#!/usr/bin/env bash
set -euo pipefail

echo "x402r-sdk pre-commit: check + build + typecheck"

pnpm check      # biome check . (lint + format)
pnpm build      # tsdown build
pnpm typecheck  # tsc --noEmit
